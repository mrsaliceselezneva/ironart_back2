<%= form_with(model: next_question, local: true) do |form| %>
  <% if next_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(next_question.errors.count, "error") %> prohibited this next_question from being saved:</h2>

      <ul>
      <% next_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
