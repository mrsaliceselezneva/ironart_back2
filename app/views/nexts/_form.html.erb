<%= form_with(model: nextt, local: true) do |form| %>
  <% if next.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nextt.errors.count, "error") %> prohibited this next from being saved:</h2>

      <ul>
      <% nextt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
